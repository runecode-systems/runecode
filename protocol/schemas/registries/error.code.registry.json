{
  "registry_name": "error.code",
  "namespace": "runecode.protocol.error.code",
  "documentation_owner": "protocol",
  "status": "mvp",
  "description": "Stable machine-consumed transport, validation, storage, auth, and runtime failure codes.",
  "codes": [
    {
      "code": "unknown_schema_id",
      "summary": "The schema_id is not registered in the local protocol bundle."
    },
    {
      "code": "unsupported_schema_version",
      "summary": "The schema_id is known but the exact schema_version is not supported."
    },
    {
      "code": "unsupported_hash_algorithm",
      "summary": "A digest reference uses a hash_alg value outside the MVP sha256 allowlist."
    },
    {
      "code": "schema_bundle_version_mismatch",
      "summary": "Communicating components do not share the same active local-session schema bundle version."
    },
    {
      "code": "stream_timeout",
      "summary": "A streamed model response exceeded the idle timeout before the next event arrived."
    },
    {
      "code": "gateway_failure",
      "summary": "The model gateway produced a terminal failure while handling the request."
    },
    {
      "code": "request_cancelled",
      "summary": "The streamed request was cancelled before a successful terminal response was produced."
    },
    {
      "code": "broker_auth_peer_credentials_required",
      "summary": "The broker rejected the request because local transport authentication credentials were unavailable or invalid."
    },
    {
      "code": "broker_api_auth_admission_denied",
      "summary": "The broker rejected the request during local admission/authentication before operation handling."
    },
    {
      "code": "broker_validation_request_id_missing",
      "summary": "The broker rejected the request because request_id was missing or empty."
    },
    {
      "code": "broker_validation_schema_invalid",
      "summary": "The broker rejected the request or response payload because it failed boundary schema validation."
    },
    {
      "code": "broker_validation_payload_base64_invalid",
      "summary": "The broker rejected the request because a base64 payload field was malformed."
    },
    {
      "code": "broker_validation_data_class_invalid",
      "summary": "The broker rejected the request because data_class was unsupported for the operation."
    },
    {
      "code": "broker_not_found_artifact",
      "summary": "The broker could not find the requested artifact for the provided digest."
    },
    {
      "code": "broker_not_found_run",
      "summary": "The broker could not find the requested run for the provided run_id."
    },
    {
      "code": "broker_limit_message_size_exceeded",
      "summary": "The broker rejected the request because encoded or decoded message size exceeded configured limits."
    },
    {
      "code": "broker_limit_structural_complexity_exceeded",
      "summary": "The broker rejected the request because JSON depth, array length, or object properties exceeded configured limits."
    },
    {
      "code": "broker_limit_in_flight_exceeded",
      "summary": "The broker rejected the request because in-flight request limits per client or lane were exceeded."
    },
    {
      "code": "broker_limit_policy_rejected",
      "summary": "The broker rejected the request due to policy-enforced runtime limits or flow controls."
    },
    {
      "code": "broker_timeout_request_deadline_exceeded",
      "summary": "The broker rejected or terminated the request because its deadline was exceeded."
    },
    {
      "code": "broker_approval_state_invalid",
      "summary": "The broker rejected approval handling because typed signed approval artifacts or required hash bindings were invalid."
    },
    {
      "code": "broker_validation_range_not_supported",
      "summary": "The broker rejected the request because artifact range reads are not supported in MVP."
    }
  ]
}
