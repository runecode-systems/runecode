{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/RunListRequest.schema.json",
  "title": "RunListRequest",
  "description": "Typed run list request with stable request_id and opaque cursor pagination.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 6,
  "required": ["schema_id", "schema_version", "request_id"],
  "properties": {
    "schema_id": { "const": "runecode.protocol.v0.RunListRequest", "description": "Protocol schema identifier.", "x-data-class": "public" },
    "schema_version": { "const": "0.1.0", "description": "Protocol schema version.", "x-data-class": "public" },
    "request_id": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[A-Za-z0-9._:-]+$", "description": "Stable request identifier.", "x-data-class": "public" },
    "cursor": { "type": "string", "minLength": 1, "maxLength": 512, "description": "Opaque pagination cursor.", "x-data-class": "public" },
    "limit": { "type": "integer", "minimum": 1, "maximum": 200, "description": "Requested page size.", "x-data-class": "public" },
    "order": { "type": "string", "enum": ["updated_at_desc", "updated_at_asc"], "description": "Explicit run ordering semantics.", "x-data-class": "public" }
  }
}
