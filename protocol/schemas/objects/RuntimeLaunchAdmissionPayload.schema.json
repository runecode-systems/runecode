{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/RuntimeLaunchAdmissionPayload.schema.json",
  "title": "RuntimeLaunchAdmissionPayload",
  "description": "Broker-owned audit payload for successful trusted runtime launch admission.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 7,
  "required": ["schema_id", "schema_version", "run_id", "backend_kind", "runtime_launch_digest", "runtime_image_descriptor_digest"],
  "properties": {
    "schema_id": { "const": "runecode.protocol.v0.RuntimeLaunchAdmissionPayload", "description": "Protocol schema identifier for runtime launch admission audit payloads.", "x-data-class": "public" },
    "schema_version": { "const": "0.1.0", "description": "Protocol schema version for runtime launch admission audit payloads.", "x-data-class": "public" },
    "run_id": { "type": "string", "minLength": 1, "maxLength": 256, "description": "Associated run identifier.", "x-data-class": "public" },
    "backend_kind": { "type": "string", "enum": ["microvm", "container"], "description": "Backend kind that passed trusted launch admission.", "x-data-class": "public" },
    "runtime_launch_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$", "maxLength": 71, "description": "Digest of persisted launch-runtime evidence.", "x-data-class": "public" },
    "runtime_image_descriptor_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$", "maxLength": 71, "description": "Digest of the admitted runtime-image descriptor.", "x-data-class": "public" },
    "runtime_toolchain_descriptor_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$", "maxLength": 71, "description": "Optional digest of the admitted runtime-toolchain descriptor.", "x-data-class": "public" }
  }
}
