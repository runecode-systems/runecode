{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/ArtifactReference.schema.json",
  "title": "ArtifactReference",
  "description": "Protocol-owned artifact reference family with shared digest, size, content-type, data-class, and provenance linkage fields.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 7,
  "required": [
    "schema_id",
    "schema_version",
    "digest",
    "size_bytes",
    "content_type",
    "data_class",
    "provenance_receipt_hash"
  ],
  "properties": {
    "schema_id": {
      "const": "runecode.protocol.v0.ArtifactReference",
      "description": "Stable protocol object-family identifier for artifact references.",
      "x-data-class": "public"
    },
    "schema_version": {
      "const": "0.4.0",
      "description": "Exact protocol schema version used to validate this artifact reference.",
      "x-data-class": "public"
    },
    "digest": {
      "$ref": "Digest.schema.json#/$defs/digestValue",
      "description": "Content-addressed digest reference for the artifact payload bytes.",
      "x-data-class": "public"
    },
    "size_bytes": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1099511627776,
      "description": "Exact artifact payload size in bytes.",
      "x-data-class": "public"
    },
    "content_type": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9!#$&^_.+-]*/[A-Za-z0-9][A-Za-z0-9!#$&^_.+-]*$",
      "description": "Declared base media type for the artifact payload. Artifact references store only the base media type and do not carry MIME parameters such as charset or boundary.",
      "x-data-class": "public"
    },
    "data_class": {
      "type": "string",
      "enum": [
        "spec_text",
        "unapproved_file_excerpts",
        "approved_file_excerpts",
        "diffs",
        "build_logs",
        "audit_events",
        "audit_verification_report",
        "audit_receipt_export_copy",
        "gate_evidence",
        "dependency_batch_manifest",
        "dependency_resolved_unit_manifest",
        "dependency_resolved_payload",
        "dependency_materialized_tree",
        "web_query",
        "web_citations"
      ],
      "description": "Immutable artifact data-class label enforced by policy and flow-matrix checks. web_query and web_citations are reserved for future role work and must remain fail-closed unless explicitly enabled by signed manifest policy.",
      "x-data-class": "public"
    },
    "provenance_receipt_hash": {
      "$ref": "Digest.schema.json#/$defs/digestValue",
      "description": "Hash of the provenance receipt that attests how this artifact was produced.",
      "x-data-class": "public"
    }
  }
}
