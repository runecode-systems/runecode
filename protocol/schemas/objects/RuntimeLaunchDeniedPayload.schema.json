{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/RuntimeLaunchDeniedPayload.schema.json",
  "title": "RuntimeLaunchDeniedPayload",
  "description": "Broker-owned audit payload for fail-closed denied runtime launch admission.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 6,
  "required": ["schema_id", "schema_version", "run_id", "backend_kind", "runtime_launch_digest", "launch_failure_reason_code"],
  "properties": {
    "schema_id": { "const": "runecode.protocol.v0.RuntimeLaunchDeniedPayload", "description": "Protocol schema identifier for runtime launch denial audit payloads.", "x-data-class": "public" },
    "schema_version": { "const": "0.1.0", "description": "Protocol schema version for runtime launch denial audit payloads.", "x-data-class": "public" },
    "run_id": { "type": "string", "minLength": 1, "maxLength": 256, "description": "Associated run identifier.", "x-data-class": "public" },
    "backend_kind": { "type": "string", "enum": ["microvm", "container"], "description": "Backend kind for the denied runtime launch.", "x-data-class": "public" },
    "runtime_launch_digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$", "maxLength": 71, "description": "Digest of persisted launch-runtime evidence for the denied launch.", "x-data-class": "public" },
    "launch_failure_reason_code": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[a-z0-9][a-z0-9._-]*$", "description": "Fail-closed reason code for denied runtime launch admission.", "x-data-class": "public" }
  }
}
