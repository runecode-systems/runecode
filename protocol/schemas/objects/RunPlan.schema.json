{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/RunPlan.schema.json",
  "title": "RunPlan",
  "description": "Immutable broker-compiled run plan binding deterministic gate, executor, and logical scope data.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 14,
  "required": [
    "schema_id",
    "schema_version",
    "plan_id",
    "run_id",
    "workflow_id",
    "process_id",
    "workflow_definition_hash",
    "process_definition_hash",
    "policy_context_hash",
    "compiled_at",
    "role_instance_ids",
    "executor_bindings",
    "gate_definitions"
  ],
  "properties": {
    "schema_id": {
      "const": "runecode.protocol.v0.RunPlan",
      "description": "Stable protocol object-family identifier for immutable run plans.",
      "x-data-class": "public"
    },
    "schema_version": {
      "const": "0.1.0",
      "description": "Exact protocol schema version used to validate this run plan.",
      "x-data-class": "public"
    },
    "plan_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "maxLength": 128,
      "description": "Stable compiled plan identity; separate from logical scope IDs and attempt IDs.",
      "x-data-class": "public"
    },
    "supersedes_plan_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "maxLength": 128,
      "description": "Optional prior plan identity superseded by this immutable plan.",
      "x-data-class": "public"
    },
    "run_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "maxLength": 128,
      "description": "Run identity bound to this plan.",
      "x-data-class": "public"
    },
    "workflow_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "maxLength": 128,
      "description": "Logical workflow identity compiled into this plan.",
      "x-data-class": "public"
    },
    "process_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "maxLength": 128,
      "description": "Logical process identity compiled into this plan.",
      "x-data-class": "public"
    },
    "workflow_definition_hash": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "maxLength": 71,
      "description": "Canonical hash of the workflow definition payload used for planning.",
      "x-data-class": "public"
    },
    "process_definition_hash": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "maxLength": 71,
      "description": "Canonical hash of the process definition payload used for planning.",
      "x-data-class": "public"
    },
    "policy_context_hash": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "maxLength": 71,
      "description": "Canonical hash of the trusted compiled policy context bound at planning time.",
      "x-data-class": "public"
    },
    "compiled_at": {
      "type": "string",
      "format": "date-time",
      "maxLength": 64,
      "description": "RFC3339 timestamp when the broker compiled this immutable plan.",
      "x-data-class": "public"
    },
    "role_instance_ids": {
      "type": "array",
      "minItems": 1,
      "maxItems": 128,
      "description": "Deterministic logical role scopes referenced by planned gate entries.",
      "x-data-class": "public",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_-]*$",
        "maxLength": 128,
        "x-data-class": "public"
      }
    },
    "executor_bindings": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "description": "Reviewed executor bindings frozen into this plan.",
      "x-data-class": "public",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "maxProperties": 5,
        "required": [
          "binding_id",
          "executor_id",
          "executor_class",
          "allowed_role_kinds"
        ],
        "properties": {
          "binding_id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_-]*$",
            "maxLength": 128,
            "description": "Stable binding identity referenced by planned gate entries.",
            "x-data-class": "public"
          },
          "executor_id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_-]*$",
            "maxLength": 128,
            "description": "Trusted executor registry identity bound for dispatch and policy validation.",
            "x-data-class": "public"
          },
          "executor_class": {
            "type": "string",
            "enum": ["workspace_ordinary", "system_modifying"],
            "description": "Execution risk class expected by policy for this binding.",
            "x-data-class": "public"
          },
          "allowed_role_kinds": {
            "type": "array",
            "minItems": 1,
            "maxItems": 16,
            "description": "Role kinds allowed to dispatch this executor binding.",
            "items": {
              "type": "string",
              "pattern": "^[a-z][a-z0-9_-]*$",
              "maxLength": 128,
              "description": "One allowed role kind for this binding.",
              "x-data-class": "public"
            },
            "x-data-class": "public"
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "maxLength": 256,
            "description": "Optional human-facing summary for operator diagnostics.",
            "x-data-class": "public"
          }
        }
      }
    },
    "gate_definitions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 128,
      "description": "Deterministic gate order, checkpoint placement, scope, and binding frozen for execution.",
      "x-data-class": "public",
      "items": {
        "$ref": "GateDefinition.schema.json",
        "x-data-class": "public"
      }
    }
  }
}
