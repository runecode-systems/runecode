{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runecode.dev/protocol/schemas/objects/BrokerArtifactHeadResponse.schema.json",
  "title": "BrokerArtifactHeadResponse",
  "description": "Typed broker artifact-head response family with request correlation and artifact reference payload.",
  "type": "object",
  "additionalProperties": false,
  "maxProperties": 4,
  "required": [
    "schema_id",
    "schema_version",
    "request_id",
    "artifact"
  ],
  "properties": {
    "schema_id": {
      "const": "runecode.protocol.v0.BrokerArtifactHeadResponse",
      "description": "Stable protocol object-family identifier for artifact head responses.",
      "x-data-class": "public"
    },
    "schema_version": {
      "const": "0.1.0",
      "description": "Exact protocol schema version used to validate this artifact head response.",
      "x-data-class": "public"
    },
    "request_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9._:-]+$",
      "description": "Stable request identifier copied from the request.",
      "x-data-class": "public"
    },
    "artifact": {
      "type": "object",
      "maxProperties": 64,
      "description": "Requested artifact reference metadata.",
      "x-data-class": "sensitive"
    }
  }
}
