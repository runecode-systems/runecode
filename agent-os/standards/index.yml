# Agent OS Standards Index

backend:
  go-cli-help-exit-codes:
    description: Go CLI help flags, streams, and exit codes
  go-cli-interactive-tty-guard:
    description: TTY guard for interactive Go CLI UIs
  go-cli-main-pattern:
    description: Go CLI `main.go` wiring-only pattern
  go-cli-stub-output:
    description: Stable Go CLI stub output and no-side-effects default

ci:
  canonical-release-artifacts:
    description: Canonical Nix-built unsigned release artifacts and publish-only CI
  github-actions-supply-chain:
    description: GitHub Actions pinning + least-privilege permissions
  just-ci:
    description: Canonical `just ci` entrypoint (check-only, cross-platform)
  nix-flake-ci-invariants:
    description: Nix flake CI cache allowlist and flake.lock invariants
  release-metadata-source-of-truth:
    description: Single-source release version, tag, binaries, and target metadata
  tag-driven-github-releases:
    description: Tag-driven GitHub Releases with protected approval, signing, and attestations
  windows-portability-matrix:
    description: Windows CI portability job and Node version matrix
  worktree-cleanliness:
    description: CI fails on tracked diffs and untracked files

global:
  dco-signoff:
    description: DCO sign-off requirement for every commit
  deterministic-check-write-tools:
    description: Deterministic tooling pattern (check-only by default, explicit write mode)
  language-aware-source-docs:
    description: Language-aware docs and anti-boilerplate comment expectations
  local-first-future-optionality:
    description: Keep local-first MVP choices out of portable logical contracts
  pr-review-finding-format:
    description: Standard PR review finding format and prioritization
  protocol-bundle-manifest:
    description: Authoritative manifest inventory for protocol schemas and registries
  protocol-canonicalization-profile:
    description: MVP JCS signing and canonicalization restrictions
  protocol-registry-discipline:
    description: Separate machine-code registries and fixture alignment rules
  protocol-schema-invariants:
    description: Fail-closed baseline for top-level protocol schemas
  repo-hygiene-baseline:
    description: Repo hygiene baseline (line endings, editorconfig, gitignore, envrc, CODEOWNERS)
  repo-local-tools:
    description: Repo-local helper tools over shell pipelines
  review-instructions-files:
    description: Conventions for `.github/instructions/*.instructions.md` files
  source-quality-enforcement-layering:
    description: Layered source-quality enforcement with golangci-lint floor and repo-specific checker
  source-quality-protected-surfaces:
    description: Protected source-quality policy, tooling, and config surfaces
  source-quality-reviewed-exceptions:
    description: Checked-in source-quality exceptions with rationale and follow-up references

javascript:
  node-engine-enforcement:
    description: Runner Node engines policy and engine-strict enforcement
  npm-ci-lockfile:
    description: Runner npm ci and package-lock determinism
  runner-node-test-runner:
    description: Runner Node built-in test runner conventions
  runner-typescript-as-lint:
    description: Runner TypeScript typecheck-as-lint (tsc --noEmit)

product:
  roadmap-conventions:
    description: Conventions for `agent-os/product/roadmap.md` (sections, spec entry format, and moving releases to Completed).

security:
  release-install-verification:
    description: Verified release install flow with checksums, signatures, and attestations
  runner-boundary-check:
    description: Runner boundary-check rules and allowed protocol access
  trust-boundary-change-checklist:
    description: Checklist for trust boundary changes (docs, guardrails, protocol)
  trust-boundary-interfaces:
    description: Allowed cross-boundary interfaces and prohibited bypasses
  trust-boundary-layered-enforcement:
    description: Trust boundary enforcement layers and guardrail expectations

testing:
  protocol-fixture-manifest-parity:
    description: Shared protocol fixture manifest and Go/JS parity
